apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: chat-backend-secret
  namespace: chat-backend
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secret-store
    kind: ClusterSecretStore
  target:
    name: chat-backend-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: shoong/chat-backend
