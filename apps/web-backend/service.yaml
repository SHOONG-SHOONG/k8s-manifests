apiVersion: v1
kind: Service
metadata:
  name: web-backend
  namespace: backend
  labels:
    app: web-backend
spec:
  selector:
    app: web-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
